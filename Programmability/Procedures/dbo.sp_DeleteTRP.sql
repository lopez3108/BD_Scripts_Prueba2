SET QUOTED_IDENTIFIER, ANSI_NULLS ON
GO

CREATE PROCEDURE [dbo].[sp_DeleteTRP] (@TRPId INT)
AS
BEGIN
  DELETE FROM TRP
  WHERE TRPId = @TRPId;

  DELETE FROM CheckListResult
  WHERE TRPId = @TRPId;
END;


GO